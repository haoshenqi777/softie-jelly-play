# 项目协作约定

## 目录与入口

| 路径 | 职责 |
| --- | --- |
| `app/` | 页面与样式；`app/page.tsx` 导出主玩法页面 |
| `app/softbody/` | 主玩法、设置面板和糖果托盘 |
| `app/lookdev/` | 外观、颜色、接触、吸收与消化样片 |
| `lib/` | 场景、渲染、糖果和交互逻辑；主场景为 `studio-scene.ts`，进食协调为 `game-intake.ts` |
| `lib/softbody/` | 体积软体、表面映射、接触、行为、表情和恢复 |
| `components/ui/`、`hooks/` | 通用界面组件与 React hooks |
| `public/` | 随应用发布的模型、纹理、WASM 和参考资源 |
| `assets/` | Blender 源文件、资源生成脚本、配方与材质来源 |
| `tools/` | 静态预览、检查和物理诊断工具 |
| `tools/wasm-compiler/` | 隔离的物理内核编译工具和生成源码 |
| `tests/` | 自动回归及固定输入；选择方法见 `docs/verification.md` |
| `third-party-licenses/` | 第三方许可文本 |
| `docs/` | 按主题维护的当前长期知识 |
| `README.md` | 运行部署、文档导航与临时文本逃生舱 |

## 开发与检查原则

- 先理解用户真实意图，不拘泥于表述；保持合理质疑，发现不合理结果时说明原因。
- 命名简洁直观，避免冗长地重复上下文。
- 优先用直接方法定位并修复根因；已经由代码逻辑确认的事实，不做形式化重复验证。
- 关注点外的变更不被默许。需要修改范围外文件或架构时，先解释原因并确认。
- 使用仓库已有 Bun 工具链，命令与运行部署说明集中在 README。
- 保留 WebGPU 渲染边界，不自动引入 WebGL 回退；改变模型、材质或玩法边界需要符合任务范围。
- 按改动选择必要的检查，不为纯文档修改运行耗时的物理回归。报告实际运行结果，不复用旧结果作为当前通过证明。
- 禁止关闭不是自己创建的、正在运行的开发服务。
- 没有明确要求时，不直接执行视觉验证；确有必要也须先得到允许。
- 不把历史会话中的发布、视觉验证或其他授权视为本次任务的授权。

## 文档原则

### 放在哪里

- `docs/` 是长期说明的统一目录，只保留当前有效、有持续参考价值的知识，按主题组织。docs 外仅保留根目录 `README.md` 和 `AGENTS.md` 两份说明性 Markdown，不在工具目录另设 README。
- README 是逃生舱：短期、未决定、过程性、尚未整理的文本，以及运行部署的辅助说明，统一在这里接纳，便于管理和提交前检查。普通执行过程可留在任务会话中，需要进入仓库时放入 README。
- AGENTS 只维护目录地图与开发、测试、文档编写的原则和约束，不承载任务进度。
- 许可证、资源来源、生成配方和机器读取的清单按用途保留在原目录，不受说明性 Markdown 的目录规则限制。

### 写什么

- 长期主题为产品、架构、物理、渲染和验证，入口见 README。先更新已有主题，确有独立知识领域时才新增文档。
- 产品描述可观察行为；架构描述模块协作；物理描述模拟与接触；渲染描述视觉与资源制作；验证描述方法与判断边界。
- 持续有效的设计理由、行为边界和操作方法可以进入 docs。逐次变更记录、执行清单、旧测试结果、阶段交接和未定提案不能进入 docs。
- 文档以长期稳定内容为主，不重复代码中的参数、依赖版本和实现清单。值得持续维护的约定即使偶尔随代码改变，也应在对应主题中同步更新。
- 历史由 Git 保存，不创建 changelog、日期报告或归档目录来保留已失效内容。删除旧文档前，提取仍有效的知识。
- 每个事实只有一个主要归属，其他位置使用链接；引用应指向仓库内可访问文件，不依赖旧会话和临时截图才能理解。
- 以单文件为主要形式。主题接近千行时先精简，内容确实庞大且可独立拆分时再建子目录，不按任务或日期分目录。

### 何时整理

- 修改影响已有文档中的行为、边界或操作方法时，同步更新对应主题；没有持续知识变化时，不要求新增文档。
- 提交前检查 README 逃生舱：长期结论迁入 docs，失效和已完成内容删除，必要的未决事项明确保留；临时文本不要求在每次提交时清零。
- 提交前检查链接、目录归属及描述与代码是否一致。文档整理不顺带修复其中记录的业务问题。
